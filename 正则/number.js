function formatNumber(num) {
  return num.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, "$&,");
}

// / g	正则表达式的开始和结束，g 代表全局匹配
// \d	匹配单个数字（0-9）
// (?= ... )	正向先行断言，匹配前面是 \d，但不包含后面的内容
// (\d{3})+	匹配 3 的倍数个连续数字（123,456 中的 456）
// \.	匹配小数点，确保千分位只加在整数部分